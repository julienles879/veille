import React from 'react';
import ArticlesList from './components/ArticlesList';

const App = () => {
    return (
        <div>
            <ArticlesList />
        </div>
    );
};

export default App;
